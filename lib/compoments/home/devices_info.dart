import 'package:flutter/material.dart';

class DevicesInfo extends StatelessWidget {
  const DevicesInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
